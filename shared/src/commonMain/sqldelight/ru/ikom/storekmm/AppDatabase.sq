CREATE TABLE Note (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL
);

insert:
INSERT INTO Note(title) VALUES(?);

selectAll:
SELECT * FROM Note;

deleteById:
DELETE FROM Note WHERE id = ?;